name: health_app
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  url_launcher: ^6.2.6   # 🌐 HARİCİ LİNK AÇMAK İÇİN EKLENDİ
  provider: ^6.1.1
  firebase_core: ^2.27.0
  firebase_auth: ^4.4.0
  http: ^0.13.6
  flutter_svg: ^1.1.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/human_model_project.svg
